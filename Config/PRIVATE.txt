# PRIVATE.txt — QWRT-CI 私有配置覆盖
#   由 VIKINGYFY 上游 Settings.sh 自动 cat 追加到 .config (在 GENERAL.txt + 设备配置之后)
#   等价于 davidtall/OpenWRT 的 Config/CUSTOM.txt, 但走 VIKINGYFY 原生 PRIVATE.txt 钩子, 0 改上游
#
#   kconfig 容忍"包不存在"的行(仅告警丢弃), 故即使个别 feed 缺包也不阻断编译。

# ============ 删除的 LuCI 插件 (上游默认可能启用, 显式关闭以精简) ============
CONFIG_PACKAGE_luci-app-homeproxy=n
CONFIG_PACKAGE_luci-app-nikki=n
CONFIG_PACKAGE_luci-app-openclash=n
CONFIG_PACKAGE_luci-app-passwall=n
CONFIG_PACKAGE_luci-app-passwall2=n
CONFIG_PACKAGE_luci-app-sing-box=n
CONFIG_PACKAGE_luci-app-gecoosac=n
CONFIG_PACKAGE_luci-app-tailscale=n
CONFIG_PACKAGE_luci-app-zerotier=n
CONFIG_PACKAGE_luci-app-vlmcsd=n
CONFIG_PACKAGE_luci-app-ddns-go=n
CONFIG_PACKAGE_luci-app-momo=n
CONFIG_PACKAGE_luci-app-mosdns=n

# ============ 启用的 LuCI 插件 ============
CONFIG_PACKAGE_luci-app-tinyfilemanager=y
CONFIG_PACKAGE_luci-app-natmapt=y
CONFIG_PACKAGE_natmapt=y
CONFIG_PACKAGE_stundeck=y
CONFIG_PACKAGE_luci-app-easytier=y

# ============ EasyTier 核心 ============
# luci-app-easytier 仅依赖 kmod-tun/luci-compat, 不自动拉 easytier 二进制包。
# 必须显式启用, 否则固件只有 LuCI/配置, 没有 /usr/bin/easytier-core。
# 注: easytier-core 二进制取决于 immortalwrt feeds 是否含 easytier 包;
#     v1 不强制注入, 若 feeds 缺包则此项为无害 no-op (详见 PRIVATE.sh 注释)。
CONFIG_PACKAGE_easytier=y

# ============ 裸核 sing-box ============
# sing-box 二进制由 PRIVATE.sh 从 reF1nd/sing-box-releases 注入到 /usr/bin/sing-box,
# 因此上游 feed 中的源码包不要编译。
CONFIG_PACKAGE_sing-box=n

# ============ reF1nd sing-box eBPF / TProxy 内核要求 ============
# beta.14 linux-arm64-musl is built with with_ebpf. shared_network uses TC
# and therefore requires cls_bpf + act_bpf from kmod-sched-bpf.
CONFIG_PACKAGE_kmod-sched-core=y
CONFIG_PACKAGE_kmod-sched-bpf=y
CONFIG_PACKAGE_kmod-nft-tproxy=y
CONFIG_PACKAGE_kmod-nft-socket=y

# ============ 设备精简：仅保留 jdcloud_re-cs-02 ============
# IPQ60XX-WIFI-YES.txt 默认启用全部 18 个 IPQ60XX 设备，此处显式关闭其余 17 个，
# 仅保留 jdcloud_re-cs-02，减少镜像产物与编译耗时。
# (本文件由 Scripts/Settings.sh 在 GENERAL + 设备配置之后 cat 追加到 .config，
#  kconfig 后值覆盖前值，make defconfig 生效；不在此处 =n 的设备仍会编译。)
# CONFIG_TARGET_DEVICE_qualcommax_ipq60xx_DEVICE_qihoo_360v6 is not set
# CONFIG_TARGET_DEVICE_qualcommax_ipq60xx_DEVICE_anysafe_e1 is not set
# CONFIG_TARGET_DEVICE_qualcommax_ipq60xx_DEVICE_cmiot_ax18 is not set
# CONFIG_TARGET_DEVICE_qualcommax_ipq60xx_DEVICE_dptech_ap3000-2c is not set
# CONFIG_TARGET_DEVICE_qualcommax_ipq60xx_DEVICE_glinet_gl-ax1800 is not set
# CONFIG_TARGET_DEVICE_qualcommax_ipq60xx_DEVICE_glinet_gl-axt1800 is not set
# CONFIG_TARGET_DEVICE_qualcommax_ipq60xx_DEVICE_jdcloud_re-ss-01 is not set
# CONFIG_TARGET_DEVICE_qualcommax_ipq60xx_DEVICE_link_nn6000-v1 is not set
# CONFIG_TARGET_DEVICE_qualcommax_ipq60xx_DEVICE_link_nn6000-v2 is not set
# CONFIG_TARGET_DEVICE_qualcommax_ipq60xx_DEVICE_linksys_mr7350 is not set
# CONFIG_TARGET_DEVICE_qualcommax_ipq60xx_DEVICE_linksys_mr7500 is not set
# CONFIG_TARGET_DEVICE_qualcommax_ipq60xx_DEVICE_philips_ly1800 is not set
# CONFIG_TARGET_DEVICE_qualcommax_ipq60xx_DEVICE_redmi_ax5 is not set
# CONFIG_TARGET_DEVICE_qualcommax_ipq60xx_DEVICE_redmi_ax5-jdcloud is not set
# CONFIG_TARGET_DEVICE_qualcommax_ipq60xx_DEVICE_sy_y6010 is not set
# CONFIG_TARGET_DEVICE_qualcommax_ipq60xx_DEVICE_xiaomi_ax1800 is not set
# CONFIG_TARGET_DEVICE_qualcommax_ipq60xx_DEVICE_zn_m2 is not set

# ============ 网络管理 / 诊断工具 ============
# nmap (7.99, immortalwrt/packages master snapshot)
CONFIG_PACKAGE_nmap=y
# net-tools: feed 拆为 net-tools-netstat / net-tools-route / mii-tool 三子包 (2.10)
CONFIG_PACKAGE_net-tools-netstat=y
CONFIG_PACKAGE_net-tools-route=y
CONFIG_PACKAGE_mii-tool=y
# nano 编辑器完整版 (9.2)
CONFIG_PACKAGE_nano-full=y

# ============ Syncthing 文件同步 ============
# feeds = immortalwrt/packages master (snapshot 预览) → syncthing 2.1.3
# (= 上游最新稳定版 v2.1.3; 上游 rc v2.1.4-rc.2 feed 不含)
# Go 包, 编译依赖 golang/host, 会增加编译时长与镜像体积。
CONFIG_PACKAGE_syncthing=y
CONFIG_PACKAGE_luci-app-syncthing=y
